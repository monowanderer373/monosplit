# Tabby Tally：正式项目与交互原型对照

源码导出日期：2026-09-17；文档整理完成：2026-09-20。正式源码：用户提供的 `dcbdb30055caa673b137a6e0d3d29763314cae5a` ZIP；视觉与演示行为：本次审查可读取的 Tabby Tally prototype 版本。本文是源码审查与实施规划，未运行正式项目测试，未连接生产数据库或部署。

## 1. 版本与发布基线

- 正式源码在本地 branch `phase5-financial-trust`，HEAD `dcbdb30`；GitHub `origin/main` 的本地引用为 `400b89b`。导出时 `HANDOFF.md` 未追踪，其余源码与 HEAD 一致。
- Phase 1–5 本地完成，Phase 5 财务规则已锁定。`202609080001_phase5_financial_trust.sql` 据 9 月 17 日的先前只读检查尚未应用到远程；当前远程状态仍需上线前重查。
- Vercel 当前生产部署与哪一个 Git SHA 对应尚未核实。不能根据时间认定部署了 `53da466`。
- ZIP 可正常解压；本次审查只读，未重跑历史记载的 233 unit、279 pgTAP、17 concurrency、23 Playwright。

## 2. 已能复用与不能照搬的地方

| 体验 | 正式代码基线 | 原型新增或变化 | 实施判断 |
|---|---|---|---|
| 朋友与空间 | `FriendsPage` / `SpaceInvitePage` / `SpacesPage` / `SpacePage` 有 Supabase 创建、邀请、成员与 RLS | 「共享」内三标签，右上角随标签切换添加／创建按钮 | 复用真实命令与权限，改呈现和入口；原型本机假邀请不能复制为真实发送 |
| 个人首页与最近记录 | `PersonalLedgerPage` 按月、分类列消费；`ledgerSummary` 得出个人份额与付款额 | B1 余额卡、统计、日常／旅行、日期分组、紧凑版、账户与标签 | 视图可逐步移植；月消费要按日期求和，不能直接把现有全量 `ledger.totals` 当本月值 |
| Quick Add | `UniversalQuickAddSheet` + `ContextGate` + `compileExpense` + outbox | 账户默认值、跨币种待补、分期、钱包扣款 | 保留 Canonical Expense 与审批流程；新增私人的账户资金腿与关联，不将钱包数据塞入展示状态 |
| 关系结算 | `SettlementPanel`、`propose_settlement`、接受／撤销事实、`F = E - T + R` | 双方各选自己的钱包、实际收付款、超付 gift/carry、跨币种、争议、收款方录入 | 对接现有结算事实；不能用模拟器替换财务授权。当前 UI 只由债务人提案，部分金额不能超过应付额；新增流程需另设计与测试 |
| 分析 | 只有个人账本统计与 Space 的 `buildTripRecap`，无独立分析路由 | 分类、趋势、排行、期间／币种窗帘式浮层、资金流 | 新增分析读模型与路由；区分个人消费、实际账户现金流、共享结算，不能从同一金额反复相加 |
| 账户 | 核心 SQL 只有 expenses、shares、contributions、settlements；无钱包及账户资金流水 | 资产／负债、期初、转账、收入／退款、FX 待补、信用卡／PayLater／贷款 | 全新个人资金子系统；现有 derived 关系余额不是银行／电子钱包余额 |
| 定期／分期 | `recurring_rules` 生成 `recurring_drafts`，用户审查后保存消费 | 到期自动记账并扣账户；分期本金还款不计新消费 | 已接受的产品规则与现有草稿语义不同；需要幂等调度、修改／撤销与账户资金联动 |

## 3. 需要特别设计的权限与账务边界

1. **新 Space 成员看旧账**。现有 `private.can_read_expense`：对于 `scope='space'`，当前有效成员可读取该 Space 账目，并未按加入日期限制。原型的「新增朋友不会自动看见之前的共享账单」若适用于新加入的 Group／Trip 成员，现有 RLS 不满足。先确认产品口径，再以新的 forward migration 与数据库权限测试落实。仅在 UI 过滤无法保护数据。
2. **私人旅行消费**。现有 `expenses` 约束 `scope='personal'` 时 `space_id IS NULL`。原型允许私人账单归属某个 Trip、只给主人可见；需要私人关联模型或 owner-only 元数据，不得改成共享 Space expense 或复制 expense。
3. **钱包不是关系余额**。现有 `payer_contributions` 与 `settlement_payments` 表示谁付款及朋友间结算，并不表示哪个真实银行账户增减。新账户记录须按账户币种、实际金额与日期登记，链接对应 canonical expense／settlement，避免支付、消费、债务被重复计数。
4. **双方账本与待确认**。原型使用本机切换 Dav／Lan 的演示视角；正式版只能由当前用户操作自己的账户。朋友的账户名称、余额和实际入账不能向另一方公开。共享提案仍依 Phase 5 授权与确认流，不因一方登记现金而提前视为双方确认。
5. **自动记账**。现有 `generate_due_recurring_drafts` 与 `respond_to_recurring_draft` 是待确认机制。用户已选择到期自动记账、离线后补记、余额不足照记并提示；要新增幂等发生键、原子记账、更正与复核提示。真实银行是否扣款未知，不可标为银行已确认。
6. **超付与尾差**。当前 `resolveSettlementAmount` 阻止结算金额超过应付额。原型支持 RM89.50 实付 RM90、余 RM0.50 礼让或下次抵扣。需要单独记录实际到账、用于清偿的份额与差额归属，且保留 Phase 5 已接受结算的不可变事实。
7. **导航决议**。本地底栏为 Personal / Friends / ＋ / Groups／Trips / Me；新原型是日常 / 分析 / ＋ / 共享 / 我的，日常内部切换旅行。用户已认可原型方向，但它覆盖了旧导航约定；要把路线与旧深链逐一映射，保留旧数据访问。

## 4. 推荐的 Cursor 实施顺序

每阶段都从本地 `dcbdb30` 向前推进；不要在 GitHub 过期的 `main` 上重做。每阶段交付代码差异、截图／预览、自动检查与未解决问题。数据库修改只能新增 migration，不改写已部署或即将部署的 Phase 5 文件。

### 0. 对照规格与本地基线

- 读 `CONTEXT.md`、ADR、Phase 5 runbook 及原型源码／演示；形成一份逐页对照矩阵和数据模型提案。
- 明确上述 Space 历史可见性、私人旅行关联、导航映射和自动入账边界；为 Preview 准备独立测试数据库方案。
- 不 push `main`，不对生产运行 SQL。将 7 个尚未推送的 commit 及当前未追踪文档单独保全。

### 1. 界面与读取层

- 建立原型导航、日常／旅行切换、B1 版面、按日期分组的记录、金额颜色、类别 icon／来源 pill、简洁模式，以及共享标签页的右上角按钮。
- 按现有 Supabase 数据呈现，不伪造 wallet 金额；真实账户功能未建好前，应标注未设置或暂不呈现账户卡。
- 接回现有 Quick Add、朋友邀请、Space 创建与深链；把月消费按实际月份与币种计算。

### 2. 私人账户与现金流

- 在新 migration 中设计 owner-only 资产账户、信用卡／PayLater／贷款、期初未知状态、账户资金流水与来源引用；定义取消／更正后的对账方式、各币种余额与只显示可用资金的首页汇总。
- 实现创建首个默认账户、付款账户、收入／退款、账户间转账、实际跨币种扣款与待补；与 canonical expense 原子或可恢复地关联。
- 用 RLS、幂等性和相同账目多钱包／不同用户的数据库测试验证，不把账户余额放在共享记录里。

### 3. 分析、计划与资金联动

- 建立独立分析页及非推挤的筛选浮层；消费、实际流入／流出和内部转账分别计算。
- 实现购买即记录全额、期数与预计到期；实际偿还时减少指定资产账户与债务，不再计一次消费；费用单独计。
- 把定期草稿扩展为用户指定的到期自动入账及补记；按发生期幂等，支持暂停／跳过／本期更正和负余额提示。

### 4. 共享、旅行与结算联动

- 实现私人旅行归属及历史成员可见规则；新加入成员、退出、已关闭旅行及旧账回归均需数据库测试。
- 结算时双方各登记自己的实际付款／收款账户；保留提案与确认前后的独立状态、部分还款、同币种 contra、超付差额、跨币种实扣和更正历史。
- Trip Close / Final Statement 属于此前规划的后续功能；不能将“旅行完成”直接等同“全部结清”。

### 5. 集成验收与上线

- 以真实用户角色在隔离环境跑完整链路：建账户 → 记个人／多人／旅行账 → 接受 → 两端结算 → 更正 → 分析／账户对账；验证匿名升级与旧记录。
- 先核实 Vercel production SHA、Preview 所连数据库和 Supabase migration history。避免 Preview 指向正式数据进行写入测试。
- 生产上线单独按 `docs/PRODUCTION_HARDENING_RUNBOOK.md` 第 7 节：新鲜备份及隔离恢复校验、Phase 5 migration、RPC／schema cache／Realtime 核验、兼容客户端、财务 canary 与监测；新账户 migrations 还需各自独立门槛。

## 5. 给 Cursor 的第一项可执行任务

**先交出对照规格，不开始全量改版**：在本地 `dcbdb30` 基线上读相关代码与 `CONTEXT.md`；将新 prototype 的页面与操作逐项映射到正式 routes、components、repositories、RPC 和新数据需求；列出本报告七项边界的明确处理方案、迁移顺序、测试计划以及每一步可能破坏的旧行为。输出计划供审核后，再按阶段 1–5 实施。不要直接 push `main`、修改已锁定的 Phase 5 migration、连接生产数据库测试，或把演示用的 `localStorage` 账本搬成正式数据源。

## 6. 审查限制

本次只读审查解压了用户的 ZIP，并读取关键页面、账务表、权限函数、定期草稿、结算界面、产品约束及原型实现；没有运行本地 Supabase／浏览器 E2E，没有核验线上服务，也没有审完所有 SQL 的完整交互矩阵。上述差异是实施规划证据，不是生产验收结论。

## 7. 可直接贴给 Cursor 的启动指令

请先读取这份对照文档、仓库的 CONTEXT.md、SOURCE_MAP.md、VERSION_CHECK.md、相关 ADR 和生产 runbook，完成正式实现前的方案整理。

本轮仅授权本地审查及新增规划文档，不授权改功能、数据库写入、push、merge 或部署。先检查当前 HEAD 是否仍为 dcbdb30055caa673b137a6e0d3d29763314cae5a；如已有新开发，报告差异并以真实当前状态分析，不 reset、不丢弃修改。

请生成 docs/PROTOTYPE_IMPLEMENTATION_PLAN.md，包含：

1. 逐功能列出已有能力、缺失能力、可复用的组件／repository／RPC，以及需要新增的表和命令。引用具体代码路径，别仅复述此报告。
2. 把已经确定的产品需求视为目标：日常／分析／＋／共享／我的导航，B1 首页，按日分组，记录显示自己的实际付款与自己的钱包，钱包余额私有，到期自动记账，以及共享结算仍需相应确认。把对旧导航和 recurring-draft 约定的替代明确记入新 ADR 草案；不要通过删除旧约束来绕开账务审查。
3. 设计私人账户账本与既有 Canonical Expense／Settlement 的关联、幂等性、权限和失败恢复。个人消费份额、实际付款、共享余额必须分清；历史记录没有钱包关联时标为未关联，不自动从新账户补扣。
4. 新成员历史账单可见性列为待用户确认事项，不擅自放宽或收紧 RLS；先列出现有行为、原型行为及实现成本。其他已确定产品需求不必再次提问。
5. 给出可逐步完成全部目标的任务序列；每步写允许修改的范围、验收用例、依赖及数据库 migration 需求。明确哪些 UI 可先落地，哪些必须等待账户或权限模型完成。
6. 对照现有 Phase 5 测试，保留不可变结算、更正链、Person／Participant、历史 Manual、权限及并发不变量。未执行的测试清楚标注，不用旧的通过数量充当本次结果。
7. 根据 runbook 给出未来隔离预览与生产发布计划；此任务不执行发布。核对 Vercel SHA、环境变量指向与 remote migration history 的缺失证据列为上线前事项。

若未收到 prototype 源码或无法打开其演示页面，利用本报告完成代码和数据规划，并明确标记无法核对的视觉尺寸、动画和交互细节；不要声称完成像素级对照。输出完成后停在计划阶段，让我将计划交给审查方核对。
